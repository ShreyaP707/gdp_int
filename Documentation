Investor-User Connection Platform Documentation
Overview
The Investor-User Connection Platform is a web application designed to facilitate connections between investors and users seeking investment opportunities. The platform provides functionalities for user registration, login, profile management, and connection history tracking.

Features
User Registration and Login: Users can create accounts and log in to access their profiles.
Investor Registration and Login: Investors can register and manage their investment details.
Profile Management: Both users and investors can update their profiles.
Connection History: Users can view their past connections with investors.
Technologies Used
Java Servlet: For handling server-side logic.
JSP (JavaServer Pages): For rendering dynamic web content.
JDBC (Java Database Connectivity): For database interactions.
MySQL: As the database management system.
Maven: For project dependency management.
Getting Started
Prerequisites
Java Development Kit (JDK) 8 or above
Apache Tomcat or another servlet container
MySQL database
Installation Steps
Clone the Repository:

bash

Run
Copy code
git clone https://github.com/ShreyaP707/gdp_int.git
cd gdp_int
Configure Database Properties: Update the db.properties file with your MySQL credentials:

properties

Run
Copy code
db.url=jdbc:mysql://localhost:3306/investor_user_db
db.user=your_username
db.password=your_password
*Build the Project:

*bash

Run
Copy code
mvn clean install
Deploy the Application: Deploy the generated WAR file located in the target directory to your servlet container (e.g., Tomcat).

*Access the Application: Start your servlet container and navigate to:


*Run
Copy code
http://localhost:8080/investor-user-platform/
*Usage Instructions
1.Registering a User:

Go to the registration page (register.jsp).
Fill in the required fields and submit the form.
Logging In:

2.Navigate to the login page (login.jsp).
Enter your credentials to log in.
Managing Your Profile:

After logging in, access your profile on the profile.jsp page to view and update your details.
3.Investor Management:

Investors can register and manage their investment details on their designated pages.
Viewing Connection History:

4.Users can check their connection history on the connectionHistory.jsp page.
Database Setup
To set up the database, execute the following SQL commands:

*sql

Run
Copy code
CREATE DATABASE investor_user_db;

USE investor_user_db;

CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    email VARCHAR(100),
    profileDetails TEXT
);

CREATE TABLE investors (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    investmentDetails TEXT
);

CREATE TABLE connections (
    id INT AUTO_INCREMENT PRIMARY KEY,
    userId INT,
    investorId INT,
    meetingLink VARCHAR(255),
    date DATETIME,
    FOREIGN KEY (userId) REFERENCES users(id),
    FOREIGN KEY (investorId) REFERENCES investors(id)
);
•Conclusion
This documentation provides a comprehensive guide to setting up and using the Investor-User Connection Platform. For further assistance, please refer to the project's GitHub repository or contact the maintainers.

